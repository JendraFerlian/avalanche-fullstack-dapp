import dotenv from "dotenv";

dotenv.config();

export const USER_PRIVATE_KEY = process.env.PRIVATE_KEY || "game gloom adapt oval leaf dove isolate soup lend faith carpet assault ticket alien student purity spawn achieve false habit kit crop urban region";